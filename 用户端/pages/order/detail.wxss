.detail-page {
  min-height: 100vh;
  background: #FAFAFA;
  padding-bottom: 48rpx;
}

.status-head {
  background: linear-gradient(180deg, #9CCDF2 0%, #6FB2E7 40%, #EAF2FA 100%);
  color: #111111;
  padding: 48rpx 32rpx 44rpx;
  border-radius: 0 0 12rpx 12rpx;
}

.status-title {
  font-size: 40rpx;
  font-weight: 700;
}

.status-sub {
  margin-top: 12rpx;
  opacity: 0.95;
}

/* 配送进度条：已接单 / 配送中 / 已送达，到达阶段圆点填满 */
.progress-card {
  margin-top: 20rpx;
}

.p-row {
  display: flex;
  align-items: flex-start;
  padding: 8rpx 12rpx 0;
}

.p-col {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  width: 120rpx;
  flex-shrink: 0;
}

.p-dot {
  width: 28rpx;
  height: 28rpx;
  border-radius: 50%;
  background: #E5EAF2;
  border: 6rpx solid #E5EAF2;
  box-sizing: border-box;
}

.p-dot.on {
  background: #3078C0;
  border-color: #D6E8FF;
}

.p-label {
  font-size: 24rpx;
  color: #999999;
}

.p-label.on {
  color: #3078C0;
  font-weight: 600;
}

.p-line {
  flex: 1;
  height: 6rpx;
  background: #E5EAF2;
  margin-top: 20rpx;
  border-radius: 3rpx;
}

.p-line.on {
  background: #3078C0;
}

.progress-sub {
  text-align: center;
  margin-top: 20rpx;
}

.pickup {
  text-align: center;
}

.pickup-label {
  font-size: 26rpx;
  color: #666666;
}

.pickup-code {
  font-size: 96rpx;
  font-weight: 700;
  letter-spacing: 16rpx;
  color: #3078C0;
  margin: 8rpx 0;
}

.pickup-tip {
  margin-top: 8rpx;
}

.info-row {
  display: flex;
  justify-content: space-between;
  padding: 12rpx 0;
  font-size: 26rpx;
  color: #666666;
}

.info-label {
  color: #111111;
  flex-shrink: 0;
  margin-right: 24rpx;
}

.shop-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding-bottom: 16rpx;
}

.shop-badge {
  width: 44rpx;
  height: 44rpx;
  border-radius: 12rpx;
  background: #DCE9F8;
  display: flex;
  align-items: center;
  justify-content: center;
}

.shop-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #111111;
}

.item {
  display: flex;
  align-items: center;
  padding: 14rpx 0;
}

.item-pic {
  width: 88rpx;
  height: 88rpx;
  border-radius: 12rpx;
  flex-shrink: 0;
}

.item-info {
  flex: 1;
  margin: 0 20rpx;
  min-width: 0;
}

.item-name {
  font-size: 28rpx;
  color: #111111;
}

.price-num-sm {
  font-size: 30rpx;
  color: var(--brand);
}

.total-row {
  display: flex;
  align-items: baseline;
  justify-content: flex-end;
  gap: 12rpx;
  margin-top: 12rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid #F0F0F0;
}

.foot-actions {
  display: flex;
  gap: 24rpx;
  padding: 16rpx 24rpx;
}
