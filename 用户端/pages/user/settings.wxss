.settings-page {
  min-height: 100vh;
  background: #FAFAFA;
  padding: 24rpx 32rpx 200rpx;
  box-sizing: border-box;
}

/* 退出登录：红色醒目，与普通设置项区分 */
.logout-cell {
  justify-content: center;
}

.logout-label {
  color: #E34D59;
  font-size: 30rpx;
  font-weight: 600;
}
