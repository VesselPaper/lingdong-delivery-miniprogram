# 真实上线所需功能书（Production Readiness）

> 项目：零栋无人送餐微信小程序（用户端 + 商家端 + 后端 + 开放物流平台对接）
> 最后更新：2026-09-08
> 本次更新依据：交接文档3（2026-09-07）——配送流程重构为「**召唤待命 + 上货定型**」、派车机制平台实测结论（**勿改回 create/direct**）、**真实微信登录已接通**、**下单超 12 件自动拆单**、邀请码校验临时放开。
> 定位：当前代码的业务逻辑闭环已真实完整，**一车多单配送批次已实现**，**真实微信登录（code2session）已接通**，但**支付 / 扫码 / 退款仍是模拟**，且**真实机器人全链路尚未真机验收**（派车机制存在待越凡确认的平台问题），缺部署与合规配置，**尚不能直接投入真实校园商业**。完成本文「上线前置清单」并逐项验收后，方可正式商用。

---

## 0. 一句话结论

**业务逻辑是真的，钱/机器人还没完全接通；身份（登录）已接通。**
购物车、下单、订单状态机、取消分级、售后、上货、取餐、店铺歇业同步、**一车多单配送批次**、**超 12 件自动拆单**等**业务逻辑已真实实现**，**真实微信登录（code2session）已启用**；但**微信支付（收款+退款）、真实扫码**仍处于"模拟"状态，**真实机器人配送全链路尚未真机验收**（派车机制已有平台实测结论，充电中拉任务等 3 项问题需越凡确认），并缺少正式部署与合规运营配套。

---

## 1. 现状盘点（哪些真、哪些模拟）

| 模块 | 当前状态 | 是否可直接商用 |
| --- | --- | --- |
| 商品/分类/库存/销量 | 真实 | ✅ |
| 购物车（加购/数量/移除） | 真实 | ✅ |
| 下单/订单状态机 | 真实 | ✅ |
| **下单超 12 件自动拆单**（贪心拆多单分批配送） | 真实 | ⚠️ 需真机验证多批配送顺序 |
| 订单取消分级（免费窗口/取消申请/商家审批） | 真实 | ✅ |
| **一车多单配送批次**（组单≤12件/召唤待命/上货定型/路径规划/批量上货/取餐计数/自动接单/自动派车） | 真实（连正式平台）；派车=**queue/create 排队 + lightTask 召唤待命 + 商家点上货按钮定型** | ⚠️ 需真机验收多地点配送顺序 + 越凡平台问题确认 |
| 售后（退款/投诉申请→商家处理→回显） | 流程真实，**退款仅本地标记** | ⚠️ 需接真实退款 |
| 商家接单/上货/配送监控 | 真实（连正式平台；开舱/关舱/开始配送等平台接口实测可用） | ⚠️ 需真机验收（机器人充电中不拉排队任务，待平台侧确认） |
| 店铺营业/歇业同步用户端、自动接单 | 真实 | ✅ |
| 取餐码/取餐页（开舱/关舱/40s自动关）；**取餐超时两段式**（15分钟未取→先送其他单→返回再等→驳回退款；打开舱门即「正在取餐」计时暂停） | 业务真实，无真实任务时本地兜底 | ⚠️ 需真机联调 + 确认「返回再等」的重新下发方式 |
| 活动/地址/点位 | 真实（点位连正式平台） | ✅ |
| **登录** | **真实（code2session 已启用）**：用户端/商家端各配 appid+secret（`WX_APPID/WX_SECRET`、`MERCHANT_WX_APPID/MERCHANT_WX_SECRET`）；无凭据才回退 demo | ⚠️ 需清理历史 `demo_` 账号 |
| **支付** | **PAY_MOCK=true 模拟** | ❌ 需真实微信支付 |
| **扫码** | **模拟扫码动作**（商家模拟扫码配单/识别机器人、用户模拟扫码取餐） | ❌ 需与真实机器人二维码打通 |
| 配送任务创建/轮询/回调 | 连正式平台；**queue/create 可用、create/direct 已停用（勿改回）**；回调未配靠 basicList 8s 轮询兜底 | ⚠️ 需真实机器人全链路 |

---

## 2. 上线前置清单（按模块）

### 2.1 微信登录（真实身份体系）✅ 已接通，剩余验收项
- 现状：`backend/server.js` 登录接口已启用**真实 code2session**（`WX_APPID/WX_SECRET` 用户端、`MERCHANT_WX_APPID/MERCHANT_WX_SECRET` 商家端，双端各自独立小程序凭据）；仅在未配置凭据时回退 `demo_ + sha1(code)` 演示登录。
- 剩余需要：
  - **清理历史 `demo_` 账号数据**（用户库中演示登录产生的 openid 均为 `demo_` 前缀）。
  - 确认用户端/商家端小程序**正式 AppID 均已认证**、版本提审通过。
  - 真机验证：真实微信授权登录 → 后端拿到真实 openid/session_key → 退出/失效正常。
  - token 策略：当前 token 即 openid（可预测、不可吊销），`RUN_MODE=production` 下启动守卫会拒绝该配置，正式上线前需换成可吊销的随机 token。
- 验收：新用户登录后后端能拿到真实 openid/session_key；退出/失效正常。

### 2.2 微信支付（真实收款 + 真实退款）❌ 未接通
- 现状：`PAY_MOCK=true` 且 `RUN_MODE=pilot` 时跳过真实微信支付直接置已支付（`server.js` 中真实支付代码 `wxpay.jsapiPay` 保留不动）；售后/取消申请"退款"仅本地标记状态。
- 需要：
  - 开通**微信支付商户号**（JSAPI 支付 + 退款权限）。
  - 配置 `backend/.env`：`WXPAY_MCHID / WXPAY_SERIAL_NO / WXPAY_PRIVATE_KEY / WXPAY_APIV3_KEY / WXPAY_NOTIFY_URL`。
  - **`RUN_MODE` 提到 `production` 即切真实收款**（`PAY_MOCK` 开关随模式切换，商户号就绪后删除）。
  - **实现真实退款**：商家"同意退款"、商家"同意取消申请"时，调用微信退款接口，把实付金额退还给用户；退款成功后才置订单 `7 已退款 / 5 已取消`。
  - 支付回调幂等；对账与异常（超时未回调）处理。
- 验收：真实下单→真实收款到商户号→真实退款到用户零钱。

### 2.3 真实扫码
- 现状：`模拟扫码取餐 / 模拟扫码配单` 只模拟"扫码动作"，扫码后的业务逻辑已完整。
- 需要：
  - 与越凡确认**机器人屏幕二维码内容**（是否含 deviceSn / 取餐码 / 任务ID / 链接）。
  - 真机 `wx.scanCode` 解析二维码 → 映射到对应接口（用户取餐 / 商家上货两个场景）。
  - 真机验证：扫机器人码 → 开舱 → 取餐/放货 → 关舱 → 派发。
- 验收：不用"模拟扫码"按钮，直接扫真机码完成全流程。

### 2.4 配送（真实机器人全链路验收，含一车多单）⚠️ 平台问题待越凡确认

**派车机制（2026-09-07 平台实测结论，接替者勿违背）：**
1. **排队任务 queue/create**：完整上货流程可用，但**机器人充电中不拉取排队任务**（机器人待机自动回充电桩 → 排队任务永远等不到车）。
2. **直接下发 create/direct**：充电中能执行，但**语义 = 货已在舱**（busyStocks 占用）→ loadingVerify 报「机器人当前不在取货阶段」；且机器人去上货点会**卡住转圈**（已紧急关闭）。
3. **drawerCtrl 可独立开关舱门**（不受任务状态限制）。
4. **结论：派车走 `queue/create`（排队）+ `lightTask` 召唤待命 + 商家点上货按钮定型。不要改回 create/direct。**

**新配送流程（多单兼容重构，Mock 冒烟全绿）：**
```
用户下单 → 商家接单（自动/手动）→ 订单并入开放批次（组单，多单合并，12 件上限）
  → 扫描(15s)检测到订单 → 召唤机器人到上货点待命（lightTask，不创建任务、不锁批次）
  → 商家端「配单上货」红点 = 待配单订单数（非批次数）
  → 商家进配单上货页，点批次「上货(N件)」→ 组单中批次此刻才定型（doDispatchBatch 创建平台任务）
  → 上货页：开舱 → 上货 → 关舱 → 弹「是否立即配送」
      ├─ 是：舱门关闭后立即配送（loadingConfirm）
      └─ 否：显示圆形「开始配送」按钮
  → 机器人规划多单路线 → 配送 → 送达 → 用户扫码取餐
```

- 现状：后端已连**正式环境**（`PLATFORM_MOCK=false`、`PLATFORM_BASE=https://robox.eventec.cn/service-open-logis`、正式三件套），召唤（lightTask）/开舱/关舱/开始配送等平台接口已实测可用；**一车多单批次**（组单→召唤待命→上货定型→批量建任务→整批上货→逐点配送→逐单取餐→批次完成）已实现并通过 Mock 冒烟（`backend/test_batch_flow.js`，全绿）。
- 需要：
  - **【最高优先 · 联系越凡】** 机器人待机自动回充电桩 → **充电中不拉 queue/create 排队任务**。需平台侧改机器人分组配置（returnStrategy/待机点）让空闲机器人能拉取；或确认 **lightTask 召唤在充电中是否生效**。
  - **【联系越凡】** lightTask 召唤 `expireTime` 格式（当前传毫秒时间戳字符串，10 分钟失效）；召唤与后续任务创建是否冲突。
  - **【联系越凡】** 平台残留任务 `1263738302861376`（订单 LD953994927427，商铺上货→东苑2栋）此前"已被机器人拉取"无法程序关闭，**测试前请平台后台处理**（防真车被误调度）。
  - **真实环境首次联调**：召唤（lightTask）与上货定型（queue/create）首次真机联调验证。
  - **真机验证一车多单多地点配送**：同批次多单在不同楼栋点位时，机器人是否按规划顺序逐点配送（当前用 priority 递减 + 创建顺序提示；若平台忽略需改用 preUnloadingTime / unionTaskId）。
  - 全链路实机验收：排队 → 去上货点 → 上货（整批开舱/放货/关舱/派发）→ 配送 → 到达取餐点 → 用户取餐 → 完成 → 批次完成。
  - **取餐超时两段式（2026-09-08 已实现）**：已送达 15 分钟未取（未打开舱门）→ 标记超时 + 重排路线先送其他单 → 其他单送完重建任务让机器人返回再等 15 分钟 → 仍没取则驳回（取消并本地退款，真实退款待商户号）；打开舱门即「正在取餐」计时暂停，防止取货中途被误判超时。**「返回再等」的重新下发方式需与越凡确认**（当前重建 queue/create 任务，是否先回上货点待验证）。
  - 回调公网可达（正式 HTTPS 域名，或 cloudflared 内网穿透，地址稳定），`PLATFORM_CALLBACK_BASE` 配置正确。
  - 异常处理：任务失败、机器人离线、取餐超时、货损等。
- 验收：真实订单由真实机器人完成配送与取餐，一车多单批次全部取完正常完成，多地点按规划顺序停靠。

### 2.5 移除/禁用全部测试辅助
上线前必须处理，防止产生脏数据或被误用：
- 商家「**测试完成配送**」接口（`/api/merchant/delivery/test-complete`）。
- **10 秒模拟送达**（`MOCK_ARRIVE_MS`，默认 10s 自动标记已送达 → 待取货）。
- 开舱/关舱/开始配送的 **DEVICE_MOCK**（运行时开关 `device_mock`，商家端 batchDetail.js/loading.js 已按该标志走模拟分支；正式后端返回 `device_mock=false` 即走真实平台调用）。
- 取餐开/关舱无真实任务时的本地兜底（`test:true`）。
- `PAY_MOCK=true` 模拟支付（`RUN_MODE=pilot`）。
- 演示登录（无凭据时的 `demo_` 回退路径；`RUN_MODE=production` 启动守卫会拒绝）。
- **邀请码校验临时放开**（`server.js` 登录接口注释掉的校验块 + 商家端 `login.wxml/login.js`，恢复方法见交接文档3 §7）。
- 本地 `backend/data/*.db` 中的测试订单/用户数据。

### 2.6 部署与域名
- 后端部署到正式服务器（Linux + Node 22+），通过 **HTTPS + 备案域名** 对外。
- 小程序后台配置 **request 合法域名**（`https://你的域名/api`）；真实地图图片来自 `eventec-k8s-pro.oss-cn-beijing.aliyuncs.com`（签名直链约 1 小时过期），需加入 **downloadFile 合法域名**。
- 环境隔离：正式/测试 `.env` 分开；凭据只存后端，**不进仓库**。
- 数据备份、日志、错误监控、进程守护（pm2/systemd）。

### 2.7 合规与运营
- 小程序注册、认证、类目与**备案**。
- 完善**隐私协议 / 用户协议**（当前为占位文案，需按真实运营主体与法务审核填写）。
- 商家（零栋铺子）**经营资质**、**食品安全责任**条款与赔付机制。
- 客服、投诉、售后响应机制（当前有售后闭环，需有人值守）。
- 支付费率与结算对账。
- 数据安全：日志脱敏、用户数据保护。

---

## 3. 分阶段上线建议

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **A · 校内试点** | 模拟支付 + 真实登录，跑通真实配送 + 真实业务闭环；一车多单 + 召唤待命/上货定型已实现（Mock 全绿），**待真机验收 + 越凡平台问题确认** | 🟢 进行中 |
| **B · 半正式试点** | 接通真实支付/退款，小范围真实交易（如一个园区） | 🟡 下一步 |
| **C · 正式商用** | 全量真实 + 真实机器人 + 合规运营 + 正式部署 | 🔴 目标 |

---

## 4. 上线验收勾选清单

- [ ] 真实微信登录（code2session）通过（已接通，清理 `demo_` 账号后验收）
- [ ] 真实微信支付收款到商户号
- [ ] 真实微信退款到用户（售后同意退款 / 取消申请同意后）
- [ ] 真实扫码取餐/配单（扫真实机器人二维码）通过
- [ ] 越凡确认三项平台问题：①机器人充电中拉取排队任务（returnStrategy/待机点或 lightTask 充电生效）②lightTask expireTime 格式与任务冲突 ③残留任务 `1263738302861376` 后台清理
- [ ] 真实环境首次联调：召唤（lightTask）→ 商家上货定型（queue/create）闭环通过
- [ ] 真实机器人全链路配送完成（含异常场景），一车多单多地点按规划顺序停靠
- [ ] 下单超 12 件自动拆单多批配送真机验证通过
- [ ] `PAY_MOCK` / 演示登录 / 测试完成配送 / 取餐兜底 / DEVICE_MOCK / 邀请码临时放开 全部移除或恢复
- [ ] 后端 HTTPS 部署 + 备案域名 + 合法域名配置
- [ ] 用户协议 / 隐私协议按真实主体完善
- [ ] 商家资质与食品安全责任条款落地
- [ ] 客服与售后值守机制就绪
- [ ] 测试数据清理，正式库独立

---

## 5. 相关代码 / 配置位置（速查）

| 事项 | 位置 |
| --- | --- |
| 真实登录 / demo 回退切换 | `backend/server.js` 登录接口；`.env` 配 `WX_APPID/WX_SECRET`（用户端）、`MERCHANT_WX_APPID/MERCHANT_WX_SECRET`（商家端）即真实登录 |
| 支付开关 | `backend/.env` → `PAY_MOCK`、`RUN_MODE`；微信支付 `backend/services/wxpay.js` |
| 退款（待实现真实退款） | 售后同意退款 `POST /api/merchant/refund/handle`；取消申请同意 `POST /api/merchant/cancel-request/handle` |
| 召唤待命（lightTask） | `backend/services/platform.js` `summonToLoadingPoint`（POST `/open-api/v1/lightTask`，expireTime 毫秒时间戳 10 分钟）；扫描调用在 `server.js` setInterval（BATCH_SCAN_MS 默认 15s） |
| 派车（queue/create） | `backend/services/platform.js` `createQueueTask`（POST `/open-api/v1/deliveryTask/queue/create`） |
| 上货定型 | 商家端 `pages/device/loading.js` `selectBatch`（status=0 批次先 POST `/merchant/delivery/batch/dispatch` 再进 batchDetail）；后端 `server.js` `doDispatchBatch`（CAS 抢占 status 0→1） |
| 关舱配送 | 商家端 `pages/device/batchDetail.js` `closeBin` / `dispatchAll`（前置校验 `phase==='loaded'`，**舱门开着禁止移动**，安全逻辑勿删） |
| 自动拆单（超 12 件） | `server.js` `splitOrderChunks` + `/api/order/create`；前端 `用户端/pages/order/confirm.js`（提示 + 逐个支付 + 跳订单列表） |
| 红点（待配单订单数） | 商家端 `pages/orders/list.js` `loadPendingCount` + `/device/pending` 返回 `pending_orders`；`pages/shop/home.js` 底部角标同源 |
| 测试辅助 | `/api/merchant/delivery/test-complete`；取餐 `pickup-open/close` 的 `test:true` 兜底；商家端 `DEVICE_MOCK`（`runtimeFlags.device_mock`）；后端 `MOCK_ARRIVE_MS` 10 秒模拟送达 |
| 邀请码临时放开 | `server.js` 登录接口（注释掉的校验块，`wantsMerchant` 临时改为 `client==='merchant'`）；商家端 `pages/user/login.wxml/login.js` |
| 店铺歇业同步 | 公开 `GET /api/shop/status`；商家 `PUT /api/merchant/shop` |
| 官方平台文档 | `接口/默认模块.openapi.json`（不入 git，本地保留） |

---

## 6. 需要花钱 / 需老师决策的事项（资金清单）

> 代码开发 0 成本；费用集中在「资质认证、平台服务、服务器/域名、机器人硬件」上。以下为正式上线涉及花钱或需要老师/负责人拍板的事项。

### 6.1 资金明细表

| # | 事项 | 用途 | 预估费用 | 说明 |
| --- | --- | --- | --- | --- |
| 1 | **微信小程序认证** | 以企业/学校主体注册小程序需认证 | **300 元/年**（企业主体；个人主体免费但受限） | 认证后才能开通支付、正式发布 |
| 2 | **微信支付商户号** | 真实收款 + 真实退款 | 开通免费；**交易手续费约 0.6%**（行业不同） | 需营业执照/主体资质，决定结算账户 |
| 3 | **云服务器** | 后端 HTTPS 部署 | **约 100–500 元/年**（学生机/轻量服务器） | 选云厂商、配置与预算 |
| 4 | **域名** | 后端访问地址 | **约 50–100 元/年** | 域名注册主体与续费 |
| 5 | HTTPS 证书 | 加密传输 | 0 元（Let's Encrypt / 云厂商免费证书） | — |
| 6 | 备案 | 国内服务器+域名需备案 | 免费（需时间与主体资质） | 以学校/公司主体备案 |
| 7 | **开放物流平台(robox)真实配送** | 机器人任务调度 | **需与越凡/平台确认**：试点是否免费？正式是否按单计费？ | 关键决策点 |
| 8 | **真实机器人** | 硬件采购/租赁 | 取决于学校是否已配置 | 采购/租赁预算 |
| 9 | 微信退款资金 | 售后/取消申请同意后退款 | 无开通费，但需先有收款流水（垫付） | 退款资金池 |
| 10 | 短信/云通知（可选） | 到货/取餐通知（当前未用） | 按量计费（通常几元/千条） | 是否启用由老师定 |

**最小资金门槛合计：约 500–900 元/年**（认证 300 + 服务器 100–500 + 域名 50–100），真实收款后另有约 0.6% 交易手续费；平台配送费与机器人硬件费用需单独确认。

### 6.2 需要老师决策的清单（决策点）

1. **注册主体**：小程序以什么主体注册？（学校 / 公司 / 个人）→ 决定认证费与能否开通支付商户号。
2. **支付**：现在是否申请微信支付商户号？（需营业执照）→ 决定能否"真实收款 / 真实退款"。
3. **手续费承担**：微信支付约 0.6% 的手续费由谁承担？（商家 / 平台 / 学校）
4. **平台费用**：与越凡确认——试点配送是否免费？正式按单收费？预算谁出？
5. **机器人硬件**：真实机器人是否已具备？是否需采购/租赁？预算？
6. **服务器/域名**：云服务器选哪家、预算多少；域名以谁名义注册。
7. **试运营资金**：是否预留一笔垫付资金（用于售后/取消的先行退款）？
8. **发布节奏**：先校内试点（模拟支付）→ 还是直接申请真实支付做小范围真实交易？
