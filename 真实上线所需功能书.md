# 真实上线所需功能书（Production Readiness）

> 项目：零栋无人送餐微信小程序（用户端 + 商家端 + 后端 + 开放物流平台对接）
> 最后更新：2026-09-03
> 定位：当前代码的业务逻辑闭环已真实完整，但**支付 / 登录 / 扫码 / 退款仍是模拟**，且缺部署与合规配置，**尚不能直接投入真实校园商业**。完成本文「上线前置清单」并逐项验收后，方可正式商用。

---

## 0. 一句话结论

**业务逻辑是真的，钱/身份/机器人还没接通。**
购物车、下单、订单状态机、取消分级、售后、上货、取餐、店铺歇业同步等**业务逻辑已真实实现**；但微信登录、微信支付（收款+退款）、真实扫码、真实机器人配送仍处于"模拟/待接通"状态，并缺少正式部署与合规运营配套。

---

## 1. 现状盘点（哪些真、哪些模拟）

| 模块 | 当前状态 | 是否可直接商用 |
| --- | --- | --- |
| 商品/分类/库存/销量 | 真实 | ✅ |
| 购物车（加购/数量/移除） | 真实 | ✅ |
| 下单/订单状态机 | 真实 | ✅ |
| 订单取消分级（免费窗口/取消申请/商家审批） | 真实 | ✅ |
| 售后（退款/投诉申请→商家处理→回显） | 流程真实，**退款仅本地标记** | ⚠️ 需接真实退款 |
| 商家接单/上货/配送监控 | 真实（连正式平台） | ⚠️ 需真机验收 |
| 店铺营业/歇业同步用户端 | 真实 | ✅ |
| 取餐码/取餐页（开舱/关舱/40s自动关） | 业务真实，无真实任务时本地兜底 | ⚠️ 需真机联调 |
| 活动/地址/点位 | 真实（点位连正式平台） | ✅ |
| **登录** | **演示登录**（demo_ 哈希） | ❌ 需接真实 code2session |
| **支付** | **PAY_MOCK=true 模拟** | ❌ 需真实微信支付 |
| **扫码** | **模拟扫码动作** | ❌ 需与真实机器人二维码打通 |
| 配送任务创建/轮询/回调 | 连正式平台 | ⚠️ 需真实机器人全链路 |

---

## 2. 上线前置清单（按模块）

### 2.1 微信登录（真实身份体系）
- 现状：`backend/server.js` 演示登录，以 `demo_ + sha1(code)` 作为 openid，无真实微信用户。
- 需要：
  - 小程序**真实 AppID + AppSecret**（用户端/商家端共用一套）。
  - 启用真实 `code2session`（server.js 中已注释保留，配置 `WX_APPID / WX_SECRET` 后启用）。
  - token 改为正式 openid 体系；清理历史 `demo_` 账号数据。
- 验收：新用户登录后后端能拿到真实 openid/session_key；退出/失效正常。

### 2.2 微信支付（真实收款 + 真实退款）
- 现状：`PAY_MOCK=true` 时跳过真实微信支付直接置已支付；售后/取消申请"退款"仅本地标记状态。
- 需要：
  - 开通**微信支付商户号**（JSAPI 支付 + 退款权限）。
  - 配置 `backend/.env`：`WXPAY_MCHID / WXPAY_SERIAL_NO / WXPAY_PRIVATE_KEY / WXPAY_APIV3_KEY / WXPAY_NOTIFY_URL`。
  - **移除 `PAY_MOCK` 开关**（删除后即走真实支付，代码已就绪）。
  - **实现真实退款**：商家"同意退款"、商家"同意取消申请"时，调用微信退款接口，把实付金额退还给用户；退款成功后才置订单 `7 已退款 / 5 已取消`。
  - 支付回调幂等；对账与异常（超时未回调）处理。
- 验收：真实下单→真实收款到商户号→真实退款到用户零钱。

### 2.3 真实扫码
- 现状：`模拟扫码取餐 / 模拟扫码配单` 只模拟"扫码动作"，扫码后的业务逻辑已完整。
- 需要：
  - 与越凡确认**机器人屏幕二维码内容**（是否含 deviceSn / 取餐码 / 任务ID / 链接）。
  - 真机 `wx.scanCode` 解析二维码 → 映射到对应接口（用户取餐 / 商家上货两个场景）。
  - 真机验证：扫机器人码 → 开舱 → 取餐/放货 → 关舱 → 派发。
- 验收：不用"模拟扫码"按钮，直接扫真机码完成全流程。

### 2.4 配送（真实机器人全链路验收）
- 现状：后端已连**正式环境**（`PLATFORM_MOCK=false`、`PLATFORM_BASE=https://robox.eventec.cn/service-open-logis`、正式三件套），任务创建/状态轮询/回调均已接入；点位已同步真实数据。
- 需要：
  - 真实机器人就绪，场地地图/点位与实际一致。
  - 全链路实机验收：排队 → 去上货点 → 上货（开舱/放货/关舱/派发）→ 配送 → 到达取餐点 → 用户取餐 → 完成。
  - 回调公网可达（正式 HTTPS 域名，或 cloudflared 内网穿透，地址稳定），`PLATFORM_CALLBACK_BASE` 配置正确。
  - 异常处理：任务失败、机器人离线、取餐超时、货损等。
- 验收：真实订单由真实机器人完成配送与取餐。

### 2.5 移除/禁用全部测试辅助
上线前必须处理，防止产生脏数据或被误用：
- 商家「**测试完成配送**」接口（`/api/merchant/delivery/test-complete`）。
- 取餐开/关舱无真实任务时的本地兜底（`test:true`）。
- `PAY_MOCK=true` 模拟支付。
- 演示登录。
- 本地 `backend/data/*.db` 中的测试订单/用户数据。

### 2.6 部署与域名
- 后端部署到正式服务器（Linux + Node 22+），通过 **HTTPS + 备案域名** 对外。
- 小程序后台配置 **request 合法域名**（`https://你的域名/api`）。
- 环境隔离：正式/测试 `.env` 分开；凭据只存后端，**不进仓库**。
- 数据备份、日志、错误监控、进程守护（pm2/systemd）。

### 2.7 合规与运营
- 小程序注册、认证、类目与**备案**。
- 完善**隐私协议 / 用户协议**（当前为占位文案，需按真实运营主体与法务审核填写）。
- 商家（零栋铺子）**经营资质**、**食品安全责任**条款与赔付机制。
- 客服、投诉、售后响应机制（当前有售后闭环，需有人值守）。
- 支付费率与结算对账。
- 数据安全：日志脱敏、用户数据保护。

---

## 3. 分阶段上线建议

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **A · 校内试点** | 模拟支付，跑通真实配送 + 真实业务闭环（当前） | 🟢 进行中 |
| **B · 半正式试点** | 接通真实登录 + 真实支付/退款，小范围真实交易（如一个园区） | 🟡 下一步 |
| **C · 正式商用** | 全量真实 + 真实机器人 + 合规运营 + 正式部署 | 🔴 目标 |

---

## 4. 上线验收勾选清单

- [ ] 真实微信登录（code2session）通过
- [ ] 真实微信支付收款到商户号
- [ ] 真实微信退款到用户（售后同意退款 / 取消申请同意后）
- [ ] 真实扫码取餐/配单（扫真实机器人二维码）通过
- [ ] 真实机器人全链路配送完成（含异常场景）
- [ ] `PAY_MOCK` / 演示登录 / 测试完成配送 / 取餐兜底 全部移除
- [ ] 后端 HTTPS 部署 + 备案域名 + 合法域名配置
- [ ] 用户协议 / 隐私协议按真实主体完善
- [ ] 商家资质与食品安全责任条款落地
- [ ] 客服与售后值守机制就绪
- [ ] 测试数据清理，正式库独立

---

## 5. 相关代码 / 配置位置（速查）

| 事项 | 位置 |
| --- | --- |
| 演示登录 / code2session 切换 | `backend/server.js`（登录接口注释保留真实实现） |
| 支付开关 | `backend/.env` → `PAY_MOCK`；微信支付 `backend/services/wxpay.js` |
| 退款（待实现真实退款） | 售后同意退款 `POST /api/merchant/refund/handle`；取消申请同意 `POST /api/merchant/cancel-request/handle` |
| 平台真实配送 | `backend/.env`（PLATFORM_*）；`backend/services/platform.js` |
| 测试辅助 | `/api/merchant/delivery/test-complete`；取餐 `pickup-open/close` 的 `test:true` 兜底 |
| 店铺歇业同步 | 公开 `GET /api/shop/status`；商家 `PUT /api/merchant/shop` |
| 官方平台文档 | `接口/默认模块.openapi.json`（不入 git，本地保留） |
