# 交接文档（项目与进度）

> 给后续对话 / 新成员快速了解项目全貌与当前进度使用。
> 最后更新：2026-09-04
> 项目根目录：`D:\01_Code\Personal\Software\送餐无人车\送餐无人车微信小程序`（git 仓库在此子目录）

---

## 0. 一句话定位

**四川师范大学成龙校区·无人送餐车小程序**：用户端（学生点餐）+ 商家端（零栋铺子接单/上货）+ Node.js 后端，对接**越凡开放物流平台（robox）正式环境**完成真实机器人配送闭环。当前处于**校内试点**阶段：支付模拟、扫码模拟、登录演示，配送已连正式平台。

---

## 1. 技术栈与结构

- 小程序：微信原生（用户端 18 页 / 商家端 19 页），UI 全自绘，仅用 `tdesign-miniprogram` 的 icon。
- 后端：Node.js 22+（内置 `node:sqlite`）+ Express，零外部数据库依赖，位于 `backend/`。
- 平台对接：`backend/services/platform.js`（任务创建/状态轮询/设备控制/点位同步），`wxpay.js`（支付 V3，未启用）。
- 目录：`backend/`、`用户端/`、`商家端/`、`doc/`（01~07 文档）、`README.md`（唯一入口）、`真实上线所需功能书.md`（上线缺口清单）。

---

## 2. 当前运行状态（快照 · 2026-09-04）

- **后端在跑**：`node server.js`，端口 3000，**真实配送模式**（`PLATFORM_MOCK=false`，正式环境 `robox.eventec.cn`，凭据已配对验证 COMM_200）。
- **店铺**：零栋铺子，**营业(open)**，`auto_accept=1`（自动接单已开）。
- **订单**：已完成 13 / 已送达 1 / 已取消 2 / 已退款 1；**无卡在配送中**的订单；配送任务：排队中 1 个、已完成 14 个。
- **待处理售后/取消申请**：均为 0。
- 商品 11 个（status=1），点位 10 个（商铺上货 + 东苑1/2/3/4/5/7/11/12/13 栋，含平台映射）。

---

## 3. 真实 vs 模拟（重要）

| 项 | 状态 |
| --- | --- |
| 业务逻辑（购物车/下单/状态机/取消分级/售后/上货/取餐/歇业同步/活动/地址/点位） | **真实** |
| 配送任务创建/状态轮询/设备控制接口 | **真实**（连正式平台） |
| 支付 | **模拟**（`PAY_MOCK=true`，真实商户号未配） |
| 登录 | **演示登录**（`demo_` 哈希，code2session 未启用） |
| 扫码 | **模拟动作**（扫码后业务逻辑真实） |
| 退款 | 本地标记（真实微信退款未接） |
| 平台回调 | `PLATFORM_CALLBACK_BASE` **未配** → 无主动回调，靠 basicList 8s 轮询兜底 |

---

## 4. 已实现功能清单（近期重点）

- **商城页**（用户端）：右列溢出修复（scroll-view 用普通 view 包裹约束宽度）、加购「+」/步进器、数量为 1 点「−」= 移除商品（商城/详情/购物车三处统一）、−/+ catchtap 不跳详情、整页不滚动+左右栏独立滚动、左分类选中高亮随右栏滚动联动、购物车栏悬浮 tabBar 之上、订单列表 tab 均分不截字。
- **订单取消分级**：免费取消窗口内（`FREE_CANCEL_WINDOW_MS` 默认 10 分钟）直接取消（不论商家是否接单、未真正配送即可）；超时未配送 → 提交**取消申请**（`cancel_requests` 表，商家同意→取消/拒绝→继续）；配送中不支持；送达后走退款/投诉。用户端取消申请页 + 商家端取消申请列表/详情处理页 + 工作台入口。
- **售后（退款/投诉）**：用户申请 → 商家处理（同意退款金额可改/拒绝需理由/回复投诉）→ 记录回显；订单状态 7 已退款。
- **商家歇业同步用户端**：公开 `GET /api/shop/status`；用户端确认订单页歇业时支付按钮置灰不可提交、店铺标签标「歇业」；商城店铺卡标「歇业」；后端 `order/create` 兜底拒绝。
- **超时未接单自动报异常（最新，本地已提交）**：真实模式定时扫描卡在「排队中/去上货点/上货中」的任务，超过 `DELIVERY_TIMEOUT_MS`（默认 15 分钟）→ 订单自动置**配送异常(6)**，商家端「异常」可见可处理；`DELIVERY_SCAN_MS` 默认 60s。
- **品牌清理**：商家端「我的」页 `SCSFDXSP`→`零栋铺子`、「享递用户协议/隐私声明」→「零栋…」（全局已无残留）。
- **文档**：README + doc/01~07 全面同步；新增《真实上线所需功能书.md》（第 6 章含**资金/老师决策清单**）。

---

## 5. Git 状态与推送策略（重要）

- **GitHub 远端**：`https://github.com/VesselPaper/lingdong-delivery-miniprogram.git`，分支 `main`。
- **GitHub 已同步到** `d225df1`（含 14 个提交：商城修复、取消流程、歇业同步、品牌清理、README）。
- **本地未推送的 3 个提交**（用户明确"不用推"）：
  1. `d4adf82` 《真实上线所需功能书》+ README 入口
  2. `7b5cd53` 功能书新增「资金与老师决策清单」
  3. `4f8f92a` 超时未接单自动报配送异常
- **推送策略**：只在用户明确说「同步」时才 push。此前推送失败过（代理 `127.0.0.1:7890` 未开 → 连不上 GitHub），可 `git -c http.proxy= -c https.proxy= push origin main` 直连重试。
- `.gitignore` 已排除：`backend/.env`、`backend/data/`、`backend/uploads/`、`接口/`（官方文档含示例凭据）、`project.private.config.json`。**真实凭据只在本地 .env，未入库**。

---

## 6. 运行与测试方式

1. **启动后端**：`cd backend && node server.js`（或双击 `启动服务器.bat`）。看到 `[lingdong-backend] listening on http://127.0.0.1:3000` 即成功。
2. **编译小程序**：微信开发者工具导入 `用户端` / `商家端` 目录编译（AppID 用测试号或自己的；`miniprogram_npm` 已提交无需构建）。
3. **测试闭环**（试点）：用户下单→模拟支付→商家接单→（真实配送：机器人上货→配送→取餐）/（无真机：商家"测试完成配送"）→ 用户取餐/确认。
4. **测试辅助**：商家「测试完成配送」`/api/merchant/delivery/test-complete`；`PAY_MOCK=true` 模拟支付；模拟扫码按钮。
5. **环境变量**（`backend/.env`，gitignored）：`PLATFORM_MOCK=false`、`PLATFORM_BASE=正式`、平台三件套、`PAY_MOCK=true`、`FREE_CANCEL_WINDOW_MS`（可覆盖默认10分钟）、`DELIVERY_TIMEOUT_MS`（默认15分钟）、`DELIVERY_SCAN_MS`。

---

## 7. 已知问题 / 待办

1. **机器人"不接单"问题（待确认）**：订单曾长期卡「排队中」未被机器人接走。已加 15 分钟超时自动标异常兜底，但**根因需确认**：机器人注册的平台环境是否与我们一致（我们=正式 `robox.eventec.cn`）。若公司测试小程序连的是 **test-robox 测试环境**，正式环境任务机器人收不到——需环境对齐。
2. **回调未配**：`PLATFORM_CALLBACK_BASE` 为空 → 无主动回调（靠轮询）；「取消/退款强制关平台任务」机制失效。上线前建议配 cloudflared 公网地址。
3. **支付/登录/扫码/退款**为模拟，见第 3 节与《真实上线所需功能书》。
4. **资金/决策**：见《真实上线所需功能书.md》第 6 章（认证 300/年、服务器、域名、支付手续费、平台配送费、机器人硬件——需老师决策）。
5. **机器人屏二维码内容**（是否含 deviceSn）待与越凡确认，用于真实扫码。

---

## 8. 关键凭据与协作

- **平台正式凭据**（appid/secret/principalId）在 `backend/.env`（本地），**不进仓库、不在此文档外泄**；成员需线下从负责人获取填入本地 `.env`。
- 微信 AppSecret / 支付商户参数未配，试点用 `PAY_MOCK=true` + 演示登录。
- 成员同步：`git pull`（README「二·8」有冲突处理说明）；`.env` 不随代码同步。
- 场地：四川师范大学成龙校区，buildingId `1257237128126592_zcsdb3dd6396c824`，mapId `..._1d-1`。

---

## 9. 文档索引

- `README.md`：启动/运行/预览/成员同步/业务逻辑说明（唯一入口）。
- `doc/01_PRD需求文档.md`、`doc/02_接口文档.md`、`doc/03_技术方案.md`、`doc/04_数据库设计.md`、`doc/05_页面路由清单.md`、`doc/06_常量字典.md`、`doc/07_后端链路文档.md`。
- `真实上线所需功能书.md`：上线缺口 + 资金/老师决策清单 + 验收勾选。
- `接口/默认模块.openapi.json`：平台官方文档（gitignored，本地）。
- 平台相关辅助：`项目进度/03_技术记录.md`、`文档/飞书文档/飞书文档.md`（不在仓库内，路径在上级目录）。

---

## 10. 本会话结束时的最后状态

- 后端正在运行（真实配送模式，店铺营业，auto_accept=1）。
- 已完成"超时未接单自动报异常"并**本地提交**（未推送）。
- 用户要求：**近期不推 GitHub**，等说「同步」再推（推送前确保代理可用或走直连）。
- 下一步最优先：**确认机器人平台环境一致性** → 用真实机器人跑通一单完整配送闭环 → 再谈回调/真实支付/扫码。
