# 交接文档（项目与进度）

> 给后续对话 / 新成员快速了解项目全貌与当前进度使用。
> 最后更新：2026-09-04（新增「一车多单」大版本）
> 项目根目录：`D:\01_Code\Personal\Software\送餐无人车\送餐无人车微信小程序`（git 仓库在此子目录）

---

## 0. 一句话定位

**四川师范大学成龙校区·无人送餐车小程序**：用户端（学生点餐）+ 商家端（零栋铺子接单/上货）+ Node.js 后端，对接**越凡开放物流平台（robox）正式环境**完成真实机器人配送闭环。当前处于**校内试点**阶段：支付模拟、扫码模拟、登录演示，配送已连正式平台。**核心差异化：一车多单配送批次（单仓最多 12 单，一条路线多地点配送）。**

---

## 1. 技术栈与结构

- 小程序：微信原生（用户端 18 页 / 商家端 19 页），UI 全自绘，仅用 `tdesign-miniprogram` 的 icon。
- 后端：Node.js 22+（内置 `node:sqlite`）+ Express，零外部数据库依赖，位于 `backend/`。
- 平台对接：`backend/services/platform.js`（任务创建/状态轮询/设备控制/点位同步/批次批量建任务/批量上货）、`backend/services/batch.js`（配送批次：组单/派车/路径规划/取餐计数/批次完成）、`wxpay.js`（支付 V3，未启用）。
- 目录：`backend/`、`用户端/`、`商家端/`、`doc/`（01~07 文档）、`README.md`（唯一入口）、`真实上线所需功能书.md`（上线缺口清单）。

---

## 2. 当前运行状态（快照 · 2026-09-04）

- **后端在跑**：`node server.js`，端口 3000，**真实配送模式**（`PLATFORM_MOCK=false`，正式环境 `robox.eventec.cn`，凭据已配对验证 COMM_200）。
- **店铺**：零栋铺子，**营业(open)**，`auto_accept=1`（自动接单已开 → 支付即自动接单并入配送批次）。
- **订单**：已完成/已取消若干；当前**待取货 2 单**（订单 31、39）、**待上货批次 2 个**（批次 2 共 4 单待上货、批次 3 含 1 单异常）、**配送中批次 1 个**（批次 1 含订单 39 已送达待取）、**配送异常订单 1 单**（订单 44，原脏点位 `??1?` 已清洗为东苑1栋，可演示异常处理）。
- **待处理售后/取消申请**：均为 0。
- 商品 10+ 个（status=1），点位 10 个（商铺上货 + 东苑1/2/3/4/5/7/11/12/13 栋，含平台映射）。
- **数据库已迁移**：新增 `delivery_batches` 表；orders 增 batch_id/picked_up_at/**goods_settled**/daily_seq；delivery_batches 增 daily_seq；delivery_tasks 增 batch_id；landmarks 增 pos_x/pos_y；users 增 order_read_at。

---

## 3. 真实 vs 模拟（重要）

| 项 | 状态 |
| --- | --- |
| 业务逻辑（购物车/下单/状态机/取消分级/售后/上货/取餐/歇业同步/活动/地址/点位/**一车多单批次**） | **真实** |
| 配送任务创建/状态轮询/设备控制接口 | **真实**（连正式平台） |
| 支付 | **模拟**（`PAY_MOCK=true`，真实商户号未配） |
| 登录 | **演示登录**（`demo_` 哈希，code2session 未启用） |
| 扫码 | **模拟动作**（商家模拟扫码配单/识别机器人、用户模拟扫码取餐；扫码后业务逻辑真实） |
| 退款 | 本地标记（真实微信退款未接） |
| 平台回调 | `PLATFORM_CALLBACK_BASE` **未配** → 无主动回调，靠 basicList 8s 轮询兜底 |

---

## 4. 已实现功能清单（近期重点）

- **一车多单（配送批次）【最新，本地已提交】**：
  - 现实模型：无人车单仓可放多份商品 → 一次出车配送一个「批次」（≤12 单）。
  - **一个仓多个人拿**：每单独立取餐码（防拿错）；`orders.picked_up_at` 记录每单是否已取走，仓内全部取完 → 批次完成；用户端展示「本车共 N 单 · 已取 X 单」。
  - **多地点配送规划**：`batch.planRoute` 按送达点位分组 + 从上货点贪心最近邻排序（近似最小化顾客总等待），停靠顺序写入 batch.route；每单创建独立平台任务（同一 deviceSn/货仓、outOrderNo/取餐码独立、priority 按停靠顺序递减）。
  - **自动接单**：营业 + auto_accept=1 → 支付即自动接单并入批次；批次满 12 单或等待期满（BATCH_WAIT_MS 默认 90s）自动派车（BATCH_SCAN_MS 默认 15s 扫描），商家也可手动派车。
  - **批量上货**：商家「配单/上货」页按批次操作（组单中派车 → 待上货选择/模拟扫码 → 整批开舱 → 放货 → 关舱 → 整批派发）；批次详情展示路线与逐单取餐码。
  - **接口**：批次列表/派车/详情、批量开舱/关舱/派发、待上货批次列表、用户端同车进度（track/order detail 带 batch）、订单红点（badge/mark-read）。
- **商家流程优化**：接单后弹窗直达「配单/上货」页（免去任务→商品→扫码配单多步）；商铺首页底部居中圆形「扫码上货」按钮（带待处理批次数角标）直达上货页。
- **商铺首页改版**：主面板四态「待接单/待上货/配送中/待取货」（无待办数字弱化，/api/merchant/stats 返回 ready_load、pickup 等），异常/售后放次面板；底部居中圆形「配单上货」按钮（白描边+按压缩放）直达上货页。
- **上货操作页改版**：顶部搜索框（批次/订单/商品/点位/收餐人过滤）；批次→订单→商品三层卡面分明；卡面标题为当日序号「批次 N / 订单 N」（完整批次编号只在单批次详情弱化显示、完整订单号只在订单详情显示）；商品行「价格在数量前」；图片用固定图盒+100%填充（修复被 flex 拉伸变形）；去掉顶部大标题/组单提示/底部模拟扫码按钮；「选择该批次上货」改实心主按钮、「派车配送」改描边按钮。
- **当日序号**：orders/delivery_batches 增 daily_seq（每天从 1 重置，历史回填），商家端卡面全部以「批次 N / 订单 N」展示。
- **任务页改版**：底部四分类「待接单/待上货/待配送/待取货」（后端 stage 过滤），删除筛选按钮与弹层；右上角「模拟扫码配单」→「配单上货」；卡面新增商品图/名称/数量与所属批次序号；状态文字按状态着色。
- **历史订单页**：卡面新增商品图/价格/数量明细行 + 批次序号/点位 + 合计。
- **配送监控**：任务行改「订单 N」+ 状态按状态着色。
- **商品管理**：标记售罄=库存置 0（新增 /api/merchant/goods/stock，修复「设 0 保存回落 999」）；商品列表显示真实已售数；新增/编辑商品分类为**选择器**（/api/merchant/goods/categories，选现有或新增）。
- **库存/销量模型**（goodsStats 服务）：下单即扣库存（防超卖、售罄拒单）；收货完成（3/4）已售+数量（goods_settled 幂等）；取消/退款未售出回补库存。
- **用户端售罄态**：商品列表/详情/购物车库存 0 时显示「售罄」、加号置灰、结算拦截；购物车接口返回 goods_stock。
- **我的页红点修复**：红点从页面右上方错位改为锚定订单图标右上角（图标内绝对定位）。
- **UI 风格统一**：app.wxss 增加全局状态色（st-*）/标签色（tag-*）/统一搜索框/btn-hover；商家端我的页卡片改 12rpx 圆角+描边（去大圆角重投影）；按钮 `box-sizing:border-box` 修复 width:100%+内边距右溢屏。
- **Round3 UI/UX 大改（本地已提交）**：
  - **上货配单页**（原「上货操作」更名）：只展示**组单中（可派车）+ 待上货（可上货）**，移除配送中区段与「待上货」分类标题；点「选择该批次上货」进入独立的**批次上货详情页**（pages/device/batchDetail，完整批次编号弱化显示 + 订单/商品完整展示，左上角返回回到批次列表、派发完成自动返回）；移除页面内「返回批次列表」按钮，无任何页面内冗余返回按钮；顶部搜索框不变。
  - **模拟开舱**：`DEVICE_MOCK=true` 时开舱/关舱/开始配送全本地模拟（按钮「模拟打开舱门」），真实平台调用代码注释保留待切换；开始配送走 `/api/merchant/device/batch/mock-dispatch` 真实推进批次→配送中（监控页可见）。
  - **任务页**：底部四分类「待接单/待上货/**配送中**/待取货」（待配送改名）；后端 `stage_text` 分类文案着色（待上货里不再显示「配送中」字样）；待上货/配送中/待取货**按批次分组**（一个批次一个卡面、批次内嵌套多单、点单进详情）。
  - **配送监控按批次分组**：配送中批次一个卡面嵌套多单 + 「已取 X/N」进度 + 商品明细，与上货配单页同布局。
  - **配送异常订单处理**：任务页异常分类（工作台异常入口）显示「重新配送 / 取消并退款」两操作；后端 `/api/merchant/order/exception/retry|refund`。
  - **订单详情状态头优化**：去掉深蓝底+彩色字，改浅色卡片+状态色圆点+状态色标题。
  - **路线/点位名清洗**：`batch.cleanName/landmarkNameOf` 剔除 `?？` 占位符（历史脏数据 `??1?` 已修复为东苑1栋，路线/订单列表不再出现乱码点位）。
  - **组件库**：`商家端/components/`（search-box/empty-view/goods-row/status-text/batch-card/order-card），app.json 全局注册，上货配单/任务页/历史订单/配送监控已接入，缩减主代码量、统一风格。
  - **用户端购物车**：底部购物栏左侧头像状圆钮不再溢出横向气泡（收入栏内）；购物车页卡片改 12rpx 圆角+描边、蓝主色、矩形结算按钮（对齐商家端历史订单页风格）。
- **Round4（本地已提交）**：
  - **导航调整**：商铺首页主面板「待上货/配送中」点击改跳**任务页对应分类**（待上货→待上货、配送中→配送中，不再跳配单页/配送监控）；配送监控页顶部「配单上货」入口按钮已删除；「异常」改跳独立**配送异常页**（pages/orders/exception，分类 全部/待处理/已处理 + 搜索 + 商品卡面 + 重新配送/取消退款，orders.exception_handled 记录处理态）。
  - **配送监控·真实地图**：新增 `GET /api/merchant/map`（platform.mapInfo：真实校园地图 map.png/barrier.png + 可通行路网 graph + 点位坐标 + eviz 机器人位置 + 活跃配送路线 + bbox 坐标映射）；监控页顶部 canvas 绘制地图/路网/路线/点位/机器人，底部图例，5 秒轮询。
  - **10 秒模拟送达**：mock-dispatch 后 `MOCK_ARRIVE_MS`（默认 10s）自动把批次内订单标记为已送达(3)/任务 70 → 直接进入「待取货」（提示「测试阶段配送时间模拟为10秒」）；**配送中订单详情移除「去配单/上货」「测试完成配送」按钮**（配送中订单无操作按钮）。
  - **任务页卡面完整商品**：四个分类卡面均完整展示商品图/价格/数量（待接单 order-card 改为逐商品 goods-row，分组卡面 showGoods=true）。
  - **用户端红点常驻**：`/api/user/order/badge` 改为**进行中订单数**（status 1/2/3）常驻计数，已读不再清除。
  - **用户订单详情配送进度条**：顶部「已接单→配送中→已送达」三段进度条（到达阶段圆点填满），删除「查看配送位置」按钮。
- **用户端 UX**：我的页订单图标红点（订单到达/状态已接单未读提示）；配送追踪页按钮精简为「刷新状态+模拟扫码取餐」不再换行；订单详情/配送追踪展示同批次进度。
- **扫码全部模拟**（测试阶段）：商家模拟扫码配单/模拟扫码识别机器人、用户模拟扫码取餐，均不调真实相机；正式上线替换为 wx.scanCode。
- **商城页**（用户端）：右列溢出修复、加购「+」/步进器、数量为 1 点「−」= 移除商品、−/+ catchtap 不跳详情、整页不滚动+左右栏独立滚动、左分类选中高亮联动、购物车栏悬浮 tabBar 之上。
- **订单取消分级**：免费取消窗口内直接取消；超时未配送提交取消申请；配送中不支持；送达后走退款/投诉。
- **售后（退款/投诉）**：用户申请 → 商家处理 → 记录回显；订单状态 7 已退款。
- **商家歇业同步用户端**：公开 `GET /api/shop/status`；歇业时支付置灰、店铺标签标「歇业」、后端拒单。
- **超时未接单自动报异常**：真实模式定时扫描卡在「排队中/去上货点/上货中」的任务，超时 → 配送异常(6)。
- **品牌清理**：商家端「我的」页与协议文案已清理为「零栋」。

---

## 5. Git 状态与推送策略（重要）

- **GitHub 远端**：`https://github.com/VesselPaper/lingdong-delivery-miniprogram.git`，分支 `main`。
- **推送策略**：只在用户明确说「同步」时才 push。此前推送失败过（代理 `127.0.0.1:7890` 未开 → 连不上 GitHub），可 `git -c http.proxy= -c https.proxy= push origin main` 直连重试。
- `.gitignore` 已排除：`backend/.env`、`backend/data/`、`backend/uploads/`、`接口/`（官方文档含示例凭据）、`project.private.config.json`。**真实凭据只在本地 .env，未入库**。
- 最近工作：一车多单批次大版本 + Round3 UI/UX 大改 + Round4（真实地图/10秒模拟送达/异常独立页/红点常驻/进度条）**本地已提交未推送**；用户要求近期不推 GitHub，等说「同步」再推。

---

## 6. 运行与测试方式

1. **启动后端**：`cd backend && node server.js`（或双击 `启动服务器.bat`）。看到 `[lingdong-backend] listening on http://127.0.0.1:3000` 即成功。
2. **编译小程序**：微信开发者工具导入 `用户端` / `商家端` 目录编译（AppID 用测试号或自己的；`miniprogram_npm` 已提交无需构建）。
3. **测试闭环**（试点）：用户下单→模拟支付→（自动接单/商家接单并入批次）→商家「上货配单」页派车→「模拟打开舱门」→关舱→立即配送（无真机：全部模拟，`mock-dispatch` 真实推进批次→配送中）→ 商家「测试完成配送」整批或用户逐单模拟扫码取餐 → 仓内取完批次完成。
4. **测试辅助**：商家「测试完成配送」支持 `order_id` / `batch_id`（整批）；`PAY_MOCK=true` 模拟支付；`DEVICE_MOCK=true` 模拟开舱/关舱/派发；配送异常演示：工作台「异常」→ 任务页异常分类「重新配送 / 取消并退款」。批次链路冒烟测试脚本：`backend/test_batch_flow.js`（Mock 模式+临时库，`node test_batch_flow.js`，已含异常处理/mock-dispatch/stage_text 断言）。
5. **环境变量**（`backend/.env`，gitignored）：`PLATFORM_MOCK=false`、`PLATFORM_BASE=正式`、平台三件套、`PAY_MOCK=true`、`FREE_CANCEL_WINDOW_MS`、`DELIVERY_TIMEOUT_MS`、`DELIVERY_SCAN_MS`、`BATCH_MAX_ORDERS`（默认12）、`BATCH_WAIT_MS`（默认90s）、`BATCH_SCAN_MS`（默认15s）、`LINGDONG_DB`（数据库路径覆盖）。

---

## 7. 已知问题 / 待办

1. **机器人"不接单"问题（待确认）**：订单曾长期卡「排队中」未被机器人接走。已加 15 分钟超时自动标异常兜底，但**根因需确认**：机器人注册的平台环境是否与我们一致（我们=正式 `robox.eventec.cn`）。若公司测试小程序连的是 **test-robox 测试环境**，正式环境任务机器人收不到——需环境对齐。**一车多单批次派车后更要确认机器人能接到同设备多任务并按路线配送**。
2. **回调未配**：`PLATFORM_CALLBACK_BASE` 为空 → 无主动回调（靠轮询）；「取消/退款强制关平台任务」机制失效。上线前建议配 cloudflared 公网地址。
3. **支付/登录/扫码/退款**为模拟，见第 3 节与《真实上线所需功能书》。扫码目前测试阶段全为「模拟扫码」按钮，正式上线需替换为真实 wx.scanCode（用户扫机器人屏二维码、商家扫机器人机身二维码），并确认机器人屏二维码内容（是否含 deviceSn）。
4. **批次路线是否被平台严格按序执行待真机验证**：当前用 priority 递减 + 创建顺序提示平台排程；若平台忽略顺序需改用 `preUnloadingTime` 预约下货时间或 unionTaskId 联合任务进一步约束。
5. **资金/决策**：见《真实上线所需功能书.md》第 6 章（认证 300/年、服务器、域名、支付手续费、平台配送费、机器人硬件——需老师决策）。

---

## 8. 关键凭据与协作

- **平台正式凭据**（appid/secret/principalId）在 `backend/.env`（本地），**不进仓库、不在此文档外泄**；成员需线下从负责人获取填入本地 `.env`。
- 微信 AppSecret / 支付商户参数未配，试点用 `PAY_MOCK=true` + 演示登录。
- 成员同步：`git pull`（README「二·8」有冲突处理说明）；`.env` 不随代码同步。
- 场地：四川师范大学成龙校区，buildingId `1257237128126592_zcsdb3dd6396c824`，mapId `..._1d-1`。

---

## 9. 文档索引

- `README.md`：启动/运行/预览/成员同步/业务逻辑/一车多单说明（唯一入口）。
- `doc/01_PRD需求文档.md`、`doc/02_接口文档.md`、`doc/03_技术方案.md`、`doc/04_数据库设计.md`、`doc/05_页面路由清单.md`、`doc/06_常量字典.md`、`doc/07_后端链路文档.md`。
- `真实上线所需功能书.md`：上线缺口 + 资金/老师决策清单 + 验收勾选。
- `接口/默认模块.openapi.json`：平台官方文档（gitignored，本地）。
- 平台相关辅助：`项目进度/03_技术记录.md`、`文档/飞书文档/飞书文档.md`（不在仓库内，路径在上级目录）。

---

## 10. 本会话结束时的最后状态

- 后端正在运行（真实配送模式，店铺营业，auto_accept=1，端口 3000）。
- 已完成「一车多单配送批次」大版本（后端 delivery_batches/批次派车/路径规划/批量上货/自动接单/自动派车、双端 UI、消息红点、模拟扫码、商家流程优化、doc 文档同步）并**本地提交**（未推送）。
- 本会话再完成「界面/商品」批次：商铺首页主面板四态+异常售后次面板+底部圆形扫码上货按钮；上货页分层卡片（批次/订单/商品）完整展示；商品分类选择器；标记售罄=库存置0+修复设0变999；收货结算已售/取消回补库存；用户端售罄置灰+号；我的页红点位置修复；doc 文档同步。**本地已提交（未推送）**。
- 批次链路已用 `backend/test_batch_flow.js` 冒烟通过（Mock 模式：下单→自动接单→组单→派车→路线→独立取餐码→扫码→整批上货→派发→整批送达→逐单取餐→批次完成；并覆盖库存扣减/售罄置0/分类/收货结算销量）。
- 用户要求：**近期不推 GitHub**，等说「同步」再推（推送前确保代理可用或走直连）。
- 下一步最优先：**确认机器人平台环境一致性 + 真机验证一车多单多地点配送顺序** → 用真实机器人跑通一单完整配送闭环 → 再谈回调/真实支付/扫码。
