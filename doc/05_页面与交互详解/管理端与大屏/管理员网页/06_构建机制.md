# 管理员网页 · 构建机制

`管理员网页/build.js` 是「拆分式构建」脚本，把按数据域拆散的小文件拼成两个浏览器直接引用的产物。

## TARGETS 清单（顺序即拼接顺序）

| 输出文件 | parts 清单 | 内容划分 |
|---|---|---|
| `admin.js` | `admin/01-core → 09-ops` 共 **9 片** | 头部 IIFE/常量/工具 → 导航与筛选 → 右键菜单 → 总览与 WS → 卡片渲染 → 刷新与认证 → 详情抽屉 → 操作日志 → 单项操作与启动 |
| `admin-map.js` | `map/01-init → 05-live` 共 5 片 | 地图初始化（元数据/坐标映射/雷达底图）→ 静态点位与配送站位 → 无人车图层 → 渲染与拉取 → 实时层 |

`admin.js` 九片的分工（与 `build.js` 里的注释逐条对应）：

| # | 文件 | 职责 |
|---|---|---|
| 01 | `js/parts/admin/01-core.js` | 头部注释 + IIFE 开头 + 常量（状态字典、活跃口径）+ 基础工具 `esc / log / toast / token / api / setAuthBanner / tag / orderTag / batchTag / taskTag / batchHeadColor / statusDotClass / lastEventText` |
| 02 | `js/parts/admin/02-nav.js` | 导航（3 项）+ 标签页 + 活跃/历史/全部分段 + 搜索/状态筛选 + `fillStatusOptions` |
| 03 | `js/parts/admin/03-menu.js` | 右键菜单（破坏性操作唯一入口）+ 机器人「更多操作」+ 复制编号 + 批次路线文本 |
| 04 | `js/parts/admin/04-overview-ws.js` | 总览渲染 + 机器人卡片 + 告警/字典 + WebSocket 事件驱动 + 导航计数 |
| 05 | `js/parts/admin/05-render-cards.js` | 配送数据卡片渲染（批次卡包裹订单子卡 / 订单卡 / 任务卡）+ 活跃/历史过滤 |
| 06 | `js/parts/admin/06-refresh-token.js` | 刷新与认证（`/state` 拉取、登录/登出/改密）+ 账号页 |
| 07 | `js/parts/admin/07-drawer.js` | 详情抽屉：状态时间线 + 实时位置 + 商品明细 |
| 08 | `js/parts/admin/08-audit.js` | 操作日志（服务端 `audit_logs`：成功与失败都在） |
| 09 | `js/parts/admin/09-ops.js` | 单项操作 + 召唤/开关舱弹窗 + 启动 + IIFE 收尾 |

## 为什么 parts 不能单独运行

- 每个 parts 文件只是同一个 IIFE 闭包里的「一段」：`01-core.js` 开头是 `(function () { 'use strict' …` 与闭包共享变量
  （`state / dataTab / dataScope / searchQ / statusFilter / liveRobots / busy / currentAdmin / onUnauthorized /
  overviewTab / drawerCtx / TOKEN_KEY`），`09-ops.js` 末尾才是启动逻辑与 `})()` 收尾；
- 中间片段无导出、无独立入口，且互相交叉引用（示例）：
  - `02-nav.js` 用 `01-core.js` 的 `$ / api`，用 `05-render-cards.js → renderDataPage`，用 `08-audit.js → loadAudit`；
  - `03-menu.js` 调 `07-drawer.js → openDrawer` 与 `09-ops.js → actCancelOrder / actCancelBatch / actCloseVoid / actVoid / actStop / actRecover / actStopCancel`，还调 `02-nav.js → navTo`；
  - `04-overview-ws.js` 调 `05-render-cards.js → renderDataPage`、`08-audit.js → loadAudit`、`07-drawer.js → renderDrawerLive`，并调用 `admin-map.js` 暴露的 `window.mapOnLive / reloadAdminMap / invalidateAdminMap`；
  - `05-render-cards.js` 用 `03-menu.js` 的 `batchRouteText / currentStopText`、`01-core.js` 的 `lastEventText`，并在内联处理器里调 `openDrawer` / `ctxXxx`；
  - `06-refresh-token.js` 给 `01-core.js` 声明的 `onUnauthorized` 赋值，并调 `04-overview-ws.js → connectWS`、`07-drawer.js → closeDrawer`；
  - `07-drawer.js` 用 `05-render-cards.js → orderGoodsRows`、`03-menu.js` 的 `copyText / batchRouteText / currentStopText / goBatchMap`；
  - `09-ops.js` 使用上述全部片段中的函数与变量；
- 单独加载任何一个片段都会因引用未定义变量抛 ReferenceError；只有 `01-core.js` 的 IIFE 头与 `09-ops.js` 的 IIFE 尾合起来才构成完整脚本。

## CUTS 常量作用

```js
const CUTS = {
  'admin.js':     [143, 281, 432, 552, 782, 900, 1089, 1181, 1413],
  'admin-map.js': [100, 158, 220, 273, 382],
}
```

- `CUTS` 记录每个输出文件各 parts 片段的「末行号」（1-based），即切块边界，单调递增；
- **正向构建 `node build.js` 只依赖 TARGETS**，按顺序把各片用 `'\n'` 相连后写出，完全不读 CUTS；
- `node build.js --gen` 才按 CUTS 边界把输出文件重新切回 parts（逐字节等价），供首次生成或直接改了产物后回拆使用；
- 行数换算式：`cut_i = 前 i 个 parts 的行数之和 + i`——每片文件以换行结尾，按 `split(/\r?\n/)` 计数时每片会多出一个空串元素；
  拼接时用 `'\n'` 相连，分隔符正好被行边界吸收，所以不再额外 `+1`。
  例：`01-core` 142 行 → `cut₁ = 143`；累计到 `09-ops`（231 行）得 `cut₉ = 1413`。
- 日常约定：只改 `js/parts/` 下的源文件，改完执行 `node build.js` 重新拼接；改过 parts 后若要继续用 `--gen`，先按上式刷新 CUTS 表。

## 已清理（原「存疑项」的收尾）

上一版文档末尾记录了两条存疑项，重构时**都已删除，不再存在**：

| 原存疑项 | 处理 |
|---|---|
| `renderPlatformPanel` 引用的容器 `#tasksPanelPlatform` 在 index.html 中不存在（遗留函数，未挂载） | 函数与平台任务表整块删除：`renderPlatformPanel`、`ctxPlat`、`runBulkPlat`、`sel.plat` 一并移除；`state.platform_tasks` 仍在（用于刷新日志计数与后端告警检测），但页面不再有平台任务表 |
| `actDelPre`（`POST /precreate/del`）已定义但未绑定任何按钮 | 函数删除；后端 `/api/admin/precreate/del` 接口保留，只是管理页不再提供入口 |

同一轮重构还清掉了这些死代码，文档里若再看到相关名字都属于陈旧内容：

- 多选批量体系：`sel / toggleSel / toggleSelAll / clearSel / updateSelUI / runBulkOrder / runBulkBatch / runBulkTask` 与批量栏；
- 批次展开：`toggleExpand / expandRow / batchOrdersCache`（改为批次卡内嵌订单子卡）；
- 历史遗留 id 与函数：`#histCount`、`#tasksSearch`、`#historySearch`、`#tasksKind / #historyKind`、`maybeAutoExpandByOrderNo`；
- 设置页手填控制权 ID 的输入框 `#ctrlId`（后端已持久化 `ctrlId`，释放时自动读取）；
- 表格渲染函数：`renderTasksPage / renderExec / renderBatchPanel / renderOrderPanel / renderLocalTaskPanel`。

顺带修正的陈旧文案：

- `01-core.js → setAuthBanner` 里的提示原为「请在上方输入正确的管理员令牌后保存」（静态令牌方案早已废弃），
  现为「请重新登录后再操作」；
- 侧栏底部原硬编码显示「服务 3000」，已删除，只留「更新 <时间>」（`#conn`）。

---

[← 返回 管理端与大屏 索引](../README.md)
