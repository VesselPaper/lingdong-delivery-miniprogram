# 07 · 分层改造进行时（server.js 迁移执行记录 · 每次会话先读这份）

> 用途：server.js 太大（原 2530 行），上下文装不下。本文件记录「当前迁移进度 + server.js 现状结构（路由/函数/行号）+ 已迁移清单 + 后续步骤」，执行 agent 只读本文件 + 按行号局部读 server.js，**不要整文件重读**。
> 创建：2026-09-17（步骤 1 执行中）。行号基于当前工作区（未提交），**每改一次 server.js 行号就会变，先跑下方「刷新行号」命令再改**。

---

## 0. 铁律与验收（来自 05/06，勿违）

- ⚠️ **每次对配送流程做代码修改后，必须先清理旧的测试订单/批次**（取消活跃订单 + 关闭平台任务 + 批次置4），再让用户重新下单测试。
- URL 一律不变（前端零改动）；db.js 表结构/迁移/种子不动；store 显式注入（无模块级单例）。
- 每步迁移完成 → `cd backend && node test_batch_flow.js` 全绿（exit 0，ExperimentalWarning 无害）→ 向用户确认再 commit/push（先 pull，禁 --force）。
- 跨域只调 service 不查表（例外：delivery 只读 orders、admin 只读聚合、cart/list JOIN goods 只读）。
- 日志已精简，勿加回刷屏；临时脚本 `_*.js` 用完即删；pwsh 内联 node -e 吃括号，一律写临时脚本。

---

## 1. 进度总览

| 步骤 | 内容 | 状态 |
|---|---|---|
| 步骤1 | user + goods 域（10+15 路由） | ✅ **完成**：test_batch_flow 全绿（exit 0）+ 专项冒烟全过（临时脚本已删） |
| 步骤2 | order 域（26 路由 + 状态机/取消/退款/拆单） | ✅ **完成**：test_batch_flow 全绿 + `_smoke_order.js` 全过（临时脚本已删） |
| 步骤3 | delivery 域（31 路由 + 4 定时器，最大） | ✅ **完成**：test_batch_flow 全绿 + `_smoke_delivery.js` 全过（含平台回调/取餐闭环/模拟到达/device 流，临时脚本已删） |
| 步骤4 | admin 域（15 admin 路由 + 2 大屏路由 + 只读聚合） | ✅ **完成**：test_batch_flow 全绿 + `_smoke_admin.js` 全过（大屏/admin 全接口 + 清理闭环，临时脚本已删） |

### 验收（06 §3）核对结果
- **URL 零变化**：git HEAD(4503706) server.js 79 条路由 vs 迁移后（server.js + domains/*/routes.js）→ **0 缺失、0 变更**；16 条"新增"均为工作区既有未提交内容（管理员工具 15 条 + pickup-by-scan 2026-09-17 需求），原样保留。
- **跨域合规**：5 个 domains/*/queries.js 只碰本域表；user 域 cartList JOIN goods 为 05 明列只读例外；delivery→orders 为 05 明列例外；admin 只读聚合例外。
- **db.js 未动**；store 显式注入（无模块级单例）；定时器经 `deliveryTimers.start(store, deps)` 单点装配；phone 脱敏/审计调用点随路由原样迁移。

### server.js 体量变化
- 原始：2530 行（git HEAD 4503706）→ 步骤1 2174 → 步骤2 1415 → 步骤3 445 → 步骤4 **130 行（纯装配层）** ✓ 达成 05 目标（~200 行内）
- 域代码落位：`domains/user`(3) `domains/goods`(3) `domains/order`(3) `domains/delivery`(4: queries/service/routes/**timers**) `domains/admin`(3) + `domains/_shared.js`
- **注意**：pwsh `(Get-Content).Count` 数字不可信；以 read 工具 / Select-String / `[System.IO.File]::ReadAllLines` 为准（本文件纯 `\n` 行尾、无 BOM）。

---

## 2. 已迁移完成的代码（步骤 1）

### 2.1 `backend/domains/_shared.js`（公共模块，全部域共用）
- 导出：`createShared(store)` → `{ auth, merchantGuard, adminGuard, audit, ok, maskPhone, toStock }`；纯函数 `ok / maskPhone / toStock / ADMIN_TOKEN` 直接导出。
- auth：token=openid 查 users；merchantGuard：auth+role==='merchant'；adminGuard：x-admin-token sha256 比对（默认 '123456'）。
- audit：写 audit_logs（失败不阻断）。**迁移后 server.js 与各域 routes 用同一套实现。**

### 2.2 `backend/domains/user/`（专属表：users / addresses / cart）
| 文件 | 内容 |
|---|---|
| queries.js | users/addresses/cart 全部 SQL；`removeCartItems` 供 order 域跨域调；cartList JOIN goods 只读 |
| service.js | `login(store, deps, body)`（演示/真实 code2session、临时放开邀请码，注释保留恢复法） |
| routes.js | 路由工厂 `(store, deps) => router`，deps={runtime}；挂载于 `/api` |

已迁移路由（URL 不变）：
`POST /api/auth/login`、`GET/PUT /api/user/profile`、`GET /api/cart/list`、`POST /api/cart/add`、`PUT /api/cart/update`、`DELETE /api/cart/remove`、`GET /api/address/list`、`POST /api/address/save`、`DELETE /api/address/delete`

### 2.3 `backend/domains/goods/`（专属表：goods / shops / activities）
| 文件 | 内容 |
|---|---|
| queries.js | goods/shops/activities 全部 SQL |
| service.js | `shopWithRuntime(shop, runtime)`（店铺+运行模式标记）；`settleSales/restoreStock` 包装 goodsStats（供 order/delivery 跨域调） |
| routes.js | 路由工厂 `(store, deps) => router`，deps={runtime}；UPLOAD_DIR = domains/goods/../../uploads |

已迁移路由：
`GET /api/goods/categories|list|detail`、`GET /api/activity/list`、`GET/PUT /api/merchant/shop`、`GET /api/shop/status`、`POST /api/merchant/upload`、`GET /api/merchant/goods`、`GET /api/merchant/goods/categories`、`PUT /api/merchant/goods/stock`、`POST /api/merchant/goods`、`PUT /api/merchant/goods`、`PUT /api/merchant/goods/status`、`GET/POST/PUT/PUT(status)/DELETE /api/merchant/activities*`

### 2.4 server.js 改动点
- 顶部新增：`const { createShared } = require('./domains/_shared')` + `userRoutes / goodsRoutes / goodsService` require（40-43 行）
- `const { auth, merchantGuard, adminGuard, audit, ok, maskPhone, toStock } = createShared(store)`（81 行，删掉了原来 5 个本地定义）
- 挂载（82-87 行）：`app.use('/api', userRoutes(store, { runtime }))`、`app.use('/api', goodsRoutes(store, { runtime }))`
- 大屏 overview 里 `shopWithRuntime(...)` 改为 `goodsService.shopWithRuntime(store.prepare(...).get(), runtime)`
- 删除了本地 `adminGuard` 定义与 `ADMIN_TOKEN` 常量（已入 _shared）

---

## 3. server.js 现状结构（2174 行，2026-09-17 步骤1 后快照）

### 3.1 区块与行号（刷新命令见 §6）
| 区块 | 行号 | 归属域 | 备注 |
|---|---|---|---|
| 头部（env 加载/require/中间件/静态/根重定向） | 1-76 | 装配层 | 保留 |
| 分层域挂载 user/goods | 82-87 | 装配层 | 保留 |
| 点位 `/api/landmarks` | 88-102 | delivery（步骤3） | 公开列表裁剪平台 ID |
| **订单**：ORDER_STATUS/REFUND_STATUS/CANCEL_REQ_STATUS、常量、公共函数、下单/支付/回调/列表/详情/取消 | 103-525 | order（步骤2） | 见 §4 |
| 取消申请（用户+商家） | 526-600 | order（步骤2） | |
| 退款/投诉（售后） | 601-689 | order（步骤2） | |
| 配送追踪 `/api/delivery/track`、`/api/delivery/confirm` | 690-810 | delivery（步骤3） | pickupContext 815 行 |
| 用户取餐（pickup-scan/code/scan/open/close） | 811-923 | delivery（步骤3） | |
| 商家统计/订单列表/详情/接单 | 925-1027 | order（步骤2） | computeStats 925 行 |
| 配送异常处理（retry/refund/列表） | 1028-1113 | order（步骤2，跨 delivery） | |
| **一车多单：配送批次**（list/detail/dispatch/open-bin/close-bin/dispatch/mock-dispatch） | 1114-1337 | delivery（步骤3） | doDispatchBatch 1136、batchCtrl 1217、MOCK_ARRIVE_MS 1319 |
| 机器人列表/配送监控/地图 | 1338-1368 | delivery（步骤3） | |
| 数据可视化大屏（map-image/overview + 缓存） | 1369-1460 | admin（步骤4） | dashCache/mapImgCache |
| 开放物流平台回调（delivery/check-order/exception） | 1461-1563 | delivery（步骤3） | callbackAuthorized 1467、platformStatusText 1511 |
| 点位同步、扫码上货（test-complete/pending/scan/open/close/dispatch） | 1564-1739 | delivery（步骤3） | getLoadingTask 1572、reconcileBatchState 1617 |
| 真实模式轮询兜底 + 超时未接单 | 1740-1783 | delivery（步骤3）定时器① ② | |
| 取餐超时两段式 | 1784-1891 | delivery（步骤3）定时器③ | moveStopToEnd 1794、revisitUnpickedOrder 1809、rejectUnpickedOrder 1827、scanPickupTimeouts 1840 |
| 批次自动派车 | 1892-1931 | delivery（步骤3）定时器④ | BATCH_SCAN_MS 1897 |
| 管理员工具（admin/* 全部） | 1932-2156 | admin（步骤4） | ADMIN_TASK_STATUS 1942 |
| listen + 启动摘要 | 2158-2174 | 装配层 | 保留 |

### 3.2 当前剩余路由逐条清单（按域，行号=当前文件）
**order 域（步骤2 要搬，~26 条）**：
`POST /api/order/create`(268) `POST /api/order/pay`(344) `POST /api/pay/notify`(380) `GET /api/order/list`(419) `GET /api/user/order/badge`(432) `POST /api/user/order/mark-read`(448) `GET /api/order/detail`(452) `POST /api/order/cancel`(504) `POST /api/order/cancel-request`(528) `GET /api/order/cancel-request/list`(545) `GET /api/merchant/cancel-requests`(553) `GET /api/merchant/cancel-request/detail`(563) `POST /api/merchant/cancel-request/handle`(570) `POST /api/refund/apply`(605) `GET /api/refund/list`(623) `GET /api/merchant/refunds`(631) `GET /api/merchant/refund/detail`(640) `POST /api/merchant/refund/handle`(646) `GET /api/merchant/stats`(944) `GET /api/merchant/orders`(951) `GET /api/merchant/order/detail`(999) `POST /api/merchant/order/confirm`(1015) `POST /api/merchant/order/exception/retry`(1032) `POST /api/merchant/order/exception/refund`(1051) `GET /api/merchant/orders/exception`(1073)

**delivery 域（步骤3 要搬，~28 条 + 4 定时器）**：
`GET /api/landmarks`(91) `GET /api/delivery/track`(691) `POST /api/delivery/confirm`(798) `POST /api/delivery/pickup-scan`(835) `POST /api/delivery/pickup-by-code`(843) `POST /api/delivery/pickup-by-scan`(857) `POST /api/delivery/pickup-open`(875) `POST /api/delivery/pickup-close`(908) `GET /api/merchant/delivery/batch/list`(1116) `GET /api/merchant/delivery/batch/detail`(1126) `POST /api/merchant/delivery/batch/dispatch`(1203) `POST /api/merchant/device/batch/open-bin`(1218) `POST /api/merchant/device/batch/close-bin`(1257) `POST /api/merchant/device/batch/dispatch`(1269) `POST /api/merchant/device/batch/mock-dispatch`(1320) `GET /api/merchant/robots`(1340) `GET /api/merchant/delivery/monitor`(1349) `GET /api/merchant/map`(1364) `POST /api/platform/callback/delivery`(1507) `POST /api/platform/cb/:token/delivery`(1509) `GET/POST /api/platform/check-order`(1549-1552) `POST /api/platform/callback/exception`(1561-1562) `POST /api/merchant/landmarks/sync`(1565) `POST /api/merchant/delivery/test-complete`(1582) `GET /api/merchant/device/pending`(1638) `POST /api/merchant/device/scan`(1652) `POST /api/merchant/device/open-bin`(1695) `POST /api/merchant/device/close-bin`(1712) `POST /api/merchant/device/dispatch`(1726)

**admin 域（步骤4 要搬，~17 条）**：
`GET /api/dashboard/map-image`(1383) `GET /api/dashboard/overview`(1403) `GET /api/admin/state`(1949) `GET /api/admin/radar`(2000) `POST /api/admin/summon`(2007) `POST /api/admin/task/cancel`(2013) `POST /api/admin/task/close`(2021) `POST /api/admin/precreate/del`(2029) `POST /api/admin/drawer`(2035) `POST /api/admin/control/grant`(2042) `POST /api/admin/control/release`(2048) `POST /api/admin/landmarks/sync`(2055) `POST /api/admin/order/cancel`(2061) `POST /api/admin/task/close-void`(2069) `POST /api/admin/batch/cancel`(2085) `POST /api/admin/reset`(2111) `POST /api/admin/task/void`(2150)

---

### 3.0 步骤2 后的 server.js 快照（1415 行，2026-09-17）
| 区块 | 行号 | 归属 |
|---|---|---|
| 头部（env/require/中间件/静态/根重定向/挂载 user+goods+order/常量引入） | 1-117 | 装配层 |
| 点位 `/api/landmarks` | 106-119 | delivery（步骤3） |
| 配送追踪（track/confirm）+ pickupContext | 121-241 | delivery |
| 用户取餐（pickup-scan/code/scan/open/close） | 242-354 | delivery |
| 一车多单：配送批次（list/detail/dispatch/open-bin/close-bin/dispatch/mock-dispatch）+ doDispatchBatch/batchCtrl/MOCK_ARRIVE_MS | 355-578 | delivery |
| 机器人列表/配送监控/地图 | 579-609 | delivery |
| 数据可视化大屏（map-image/overview + 缓存） | 610-701 | admin（步骤4） |
| 开放物流平台回调（delivery/check-order/exception） | 702-804 | delivery |
| 点位同步、扫码上货（test-complete/pending/scan/open/close/dispatch）+ getLoadingTask/reconcileBatchState | 805-980 | delivery |
| 真实模式轮询兜底 + 超时未接单（定时器①②） | 981-1024 | delivery |
| 取餐超时两段式（定时器③）+ moveStopToEnd/revisitUnpickedOrder/rejectUnpickedOrder/scanPickupTimeouts | 1025-1132 | delivery |
| 批次自动派车（定时器④）BATCH_SCAN_MS | 1133-1172 | delivery |
| 管理员工具（admin/* 全部）+ ADMIN_TASK_STATUS | 1173-1396 | admin（步骤4） |
| listen + 启动摘要 | 1397-1415 | 装配层 |

### 3.1 步骤1/2 后的 route/function 逐条清单（行号=当前 1415 行版）
- **delivery 路由**：`/api/landmarks`(109) `/api/delivery/track`(122) `/api/delivery/confirm`(229) `/api/delivery/pickup-scan`(266) `/api/delivery/pickup-by-code`(274) `/api/delivery/pickup-by-scan`(288) `/api/delivery/pickup-open`(306) `/api/delivery/pickup-close`(339) `/api/merchant/delivery/batch/list`(357) `/api/merchant/delivery/batch/detail`(367) `/api/merchant/delivery/batch/dispatch`(444) `/api/merchant/device/batch/open-bin`(459) `/api/merchant/device/batch/close-bin`(498) `/api/merchant/device/batch/dispatch`(510) `/api/merchant/device/batch/mock-dispatch`(561) `/api/merchant/robots`(581) `/api/merchant/delivery/monitor`(590) `/api/merchant/map`(605) `/api/platform/callback/delivery`(748) `/api/platform/cb/:token/delivery`(750) `/api/platform/check-order`(790-793) `/api/platform/callback/exception`(802-803) `/api/merchant/landmarks/sync`(806) `/api/merchant/delivery/test-complete`(823) `/api/merchant/device/pending`(879) `/api/merchant/device/scan`(893) `/api/merchant/device/open-bin`(936) `/api/merchant/device/close-bin`(953) `/api/merchant/device/dispatch`(967)
- **delivery 函数**：pickupContext(246) doDispatchBatch(377) batchCtrl(458) MOCK_ARRIVE_MS(560) callbackAuthorized(708) cbDenied(719) handleDeliveryCallback(725) platformStatusText(752) handleCheckOrder(764) handleExceptionCallback(796) getLoadingTask(813) reconcileBatchState(858) moveStopToEnd(1035) revisitUnpickedOrder(1050) rejectUnpickedOrder(1068) scanPickupTimeouts(1081) BATCH_SCAN_MS(1138)
- **定时器**：①轮询兜底 8s（981 起 realPlatform 块内）②scanStuckDeliveries 60s ③scanPickupTimeouts 30s（1889 原位置，现 ~1130）④自动派车 15s（1133 起）
- **admin 路由（步骤4）**：`/api/dashboard/map-image`(624) `/api/dashboard/overview`(644) `/api/admin/state`(1190) `/api/admin/radar`(1241) `/api/admin/summon`(1248) `/api/admin/task/cancel`(1254) `/api/admin/task/close`(1262) `/api/admin/precreate/del`(1270) `/api/admin/drawer`(1276) `/api/admin/control/grant`(1283) `/api/admin/control/release`(1289) `/api/admin/landmarks/sync`(1296) `/api/admin/order/cancel`(1302) `/api/admin/task/close-void`(1310) `/api/admin/batch/cancel`(1326) `/api/admin/reset`(1352) `/api/admin/task/void`(1391)

### 3.2 迁移注意事项（步骤3 专属）
- server.js 现从 orderService 引入：`ORDER_STATUS / PICKUP_* / DELIVERY_*` 常量与 `orderDeps={orderCancel,platform}`（applyOrderCancelled/realRefundOrLocal 的依赖）。
- 步骤3 搬完后这些引入全部随 delivery 一起迁走；`goodsStats.settleSales`（delivery/confirm、test-complete 在用）改走 goodsService.settleSales；`batch.*` 并入 delivery.service。
- `orderCancel` 与 `platform` 仍为公共 service（order.service/delivery 共用）。

## 4. server.js 剩余公共函数/常量（步骤 2/3/4 迁移时逐个搬）

| 函数/常量 | 行号 | 归属 | 要点 |
|---|---|---|---|
| ORDER_STATUS / REFUND_STATUS / CANCEL_REQ_STATUS | 104/109/112 | order | 状态字典 |
| FREE_CANCEL_WINDOW_MS | 116 | order | env 可覆盖 |
| PICKUP_REOPEN_WINDOW_MS / MAX_PICKUP_OPEN | 120-121 | delivery | 取餐重开 |
| PICKUP_TIMEOUT_MS / RETRY / PICKING_GUARD / SCAN | 129-132 | delivery | 取餐超时 |
| DELIVERY_TIMEOUT_MS / DELIVERY_SCAN_MS | 138-139 | delivery/order | 卡死阈值 |
| orderTrulyDelivering(order) | 142 | order | 任务>=50 才算真配送 |
| orderStuckDelivering(order) | 153 | order | 卡死放行退款 |
| maybeAutoAccept(store, order) | 166 | order | 调 batch.addOrderToBatch（跨 delivery） |
| withinFreeCancelWindow(order) | 184 | order | |
| applyOrderCancelled(order, opts) | 196 | order | 委托 services/orderCancel + platform 召回 |
| realRefundOrLocal(order, refundId, amount) | 223 | order | wxpay 或本地标记 |
| splitOrderChunks(lineItems, maxItems) | 246 | order | 12 件拆单 |
| pickupContext(store, order) | 815 | delivery | 取餐上下文 |
| computeStats() | 925 | order | 商家 stats + 大屏共用 |
| doDispatchBatch(store, batchId, deviceSn) | 1136 | delivery | CAS 0→1 派车 + Route B 建任务 |
| batchCtrl Map | 1217 | delivery | batchId → {ctrlId, deviceSn} 内存 |
| MOCK_ARRIVE_MS | 1319 | delivery | 模拟送达 |
| callbackAuthorized / cbDenied | 1467/1478 | delivery | 回调防伪（crypto.timingSafeEqual） |
| handleDeliveryCallback / handleCheckOrder / handleExceptionCallback | 1484/1523/1555 | delivery | 平台回调 |
| platformStatusText(code) | 1511 | delivery | 任务状态字典（0-150） |
| getLoadingTask(body) | 1572 | delivery | 单任务扫码上货（旧路径） |
| reconcileBatchState(store, batchId) | 1617 | delivery | 批次状态重算 |
| moveStopToEnd / revisitUnpickedOrder / rejectUnpickedOrder / scanPickupTimeouts | 1794/1809/1827/1840 | delivery | 取餐超时逻辑 |
| BATCH_SCAN_MS | 1897 | delivery | 自动派车扫描 |
| ADMIN_TASK_STATUS | 1942 | admin | 管理员页字典 |

**定时器（全部归 delivery/timers.js，步骤3）**：
1. 轮询兜底 8s（1740 行附近 `if (runtime.realPlatform)` 块内）
2. 超时未接单 60s（同块 scanStuckDeliveries）
3. 取餐超时 30s（1840 scanPickupTimeouts，2238 行附近 setInterval 在 §3.1 的 1784-1891 段）
4. 批次自动派车/模拟到达 15s（1892-1931）

**services/ 现状**：platform.js（1322 行，公共，不动）、batch.js（归 delivery，步骤3 并入）、goodsStats.js（归 goods，已包装）、orderCancel.js（归 order，步骤2 并入或保留为 order 域实现）、runtime.js、wxpay.js（公共，不动）。

---

## 5. 下一步执行指引

### 步骤 2：order 域（做完后 server.js 应再减 ~500 行）
1. 建 `domains/order/{queries,service,routes}.js`：queries 只碰 orders/order_items/refunds/cancel_requests/pay_notifications；**读 delivery_batches/tasks 走 delivery service 或只读查询（跨域只读注释）**。
2. 搬 §3.2 order 域全部 26 条路由；状态字典、FREE_CANCEL_WINDOW_MS、orderTrulyDelivering、orderStuckDelivering、maybeAutoAccept、withinFreeCancelWindow、applyOrderCancelled、realRefundOrLocal、splitOrderChunks、computeStats 搬入 order.service。
3. 跨域调用：maybeAutoAccept/confirm → `delivery.service.addToBatch`（步骤3 前先调 `services/batch.js` 保持兼容，05 §5-2 已注明）；取消回补库存 → `goodsService.restoreStock`；删购物车 → `user.service.removeCartItems`。
4. 回归：test_batch_flow 全绿 + 真机下单/支付/取消/退款冒烟 → 向用户确认再提交。

### 步骤 3：delivery 域
- 抽 delivery.queries（delivery_batches/delivery_tasks/landmarks）；batch.js 并入 delivery.service；4 定时器迁 timers.js（server.js 末尾 start() 一次）；applyStatus 保留在 delivery 但订单推进调 order.service。
- 遗留注意：`recreatePickupTask` 仍 queue/create（排队机制已死，待改 Route B 或越凡确认）；多单直接下发语义未真机验证。

### 步骤 4：admin 域
- admin.routes 收编 /api/admin/* 与 /api/dashboard/*；admin.service 调各域 service + platform；大屏聚合保留只读 SQL；computeStats 从 order.service 引入。

---

## 6. 刷新行号命令（每次改 server.js 前先跑）

```powershell
$f = "D:\01_Code\Personal\Software\送餐无人车\校内快递配送机器人\backend\server.js"
# 区块头
Select-String -Path $f -Pattern "^// ----------" | % { "$($_.LineNumber): $($_.Line.Trim())" }
# 路由
Select-String -Path $f -Pattern "^app\.(get|post|put|delete)\(" | % { "$($_.LineNumber): $($_.Line.Trim().Substring(0,[Math]::Min(90,$_.Line.Trim().Length)))" }
# 函数/常量
Select-String -Path $f -Pattern "^(async )?function |^const (ORDER_STATUS|REFUND_STATUS|CANCEL_REQ_STATUS|FREE_CANCEL_WINDOW_MS|PICKUP_|MAX_PICKUP|DELIVERY_|MOCK_ARRIVE|BATCH_SCAN_MS|ADMIN_TASK_STATUS|batchCtrl|dashCache|mapImgCache)" | % { "$($_.LineNumber): $($_.Line.Trim().Substring(0,[Math]::Min(80,$_.Line.Trim().Length)))" }
# 总行数用 read 工具看（pwsh Get-Content 计数不可信）
```

## 7. 文档索引（理思路文件夹）
00 总览与决策 → 01 数据项地图 → 02/03/04 链路追踪 → 05 分层改造方案（★迁移蓝图） → 06 回归与验证清单 → **07 本文件（改造进行时）**。交接文档6 为会话级交接总纲。

---

## 8. 成员功能合并记录（2026-09-17，本地基线 ce347be + 合并 88f6b08 之后）

成员远端（origin/main：`bf32c90` 商家邀请码 / `1c5c0ea` 交接文档6+大屏雷达 / `9738ac7` 优惠活动类型化）已合并，新功能**按本架构落位**（第二轮提交）：

| 功能 | 落位 | 说明 |
|---|---|---|
| 优惠活动类型化（discount/full_reduce/custom + 选券） | `services/promotion.js`（共享，纯函数）+ **order 域** createOrder 权威计价 + **goods 域** 展示/商家管理 + **user 域** cart/list 折后价 | URL 不变：`/api/activity/list`（type/config/state）、`/api/goods/list|detail`（sale_price/discount_info）、`/api/cart/list`（price_now）、`/api/order/create`（activity_id → original/discount/activity 落库） |
| 商家邀请码 + 登录限流 | `services/merchantInvite.js` + `tools/merchant_invite.js` + **user 域** login（clientIp + loginFail 20/10min） | 一码一 openid 首登绑定、可吊销；MERCHANT_INVITE_CODE 仅兜底；runtime.js 已放开硬阻塞 |
| 天地图 tk 配置接口 | **admin 域** `/api/config/tianditu`（公开） | 大屏用；本地未配 TIANDITU_TK 返回空 |
| db.js 兼容增量 | 仅新增：orders 3 列、activities 4 列、merchant_invites 表 | 与本地 ctrl_id 列并存 |
| platform.js | 成员底图签名重试 + 本地 Route B syncLoading=0/listPlatformTasks 并存 | 无冲突 |
| 启动脚本 | `启动服务器.bat` 取成员版（GBK+CRLF，.gitattributes 保证） | 双端 config.js 保留本地 IP `10.75.178.169` |
| 大屏 radarMap/digital | **暂缓未合**（用户决定）；本地保持高德底图+标定 | 文件仍在 origin/main，需要时单独合 |

回归：`node test_batch_flow.js` exit 0 + `_smoke_merge.js`（登录/邀请码/限流/活动/折后价/权威计价 21 项）全绿，临时脚本已删。
